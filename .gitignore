# virtual environment - never commit this, everyone builds their own
.venv/
venv/
env/

# python bytecode
__pycache__/
*.pyc

# files the game generates at runtime
diveplan.json
divelog.txt
catalog.csv
*.save
_smoke*.json

# the solution/ scripts import the canonical engine.py via a sys.path shim,
# so they never need a copy here - block a stray one from being committed
solution/engine.py

# editor / OS junk
.vscode/
.idea/
.DS_Store
Thumbs.db
